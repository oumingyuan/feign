spring:
  application:
    name: feign-consumer

  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        health-check-path: /health
        health-check-interval: 5s
        ip-address: 127.0.0.1
        prefer-ip-address: true
