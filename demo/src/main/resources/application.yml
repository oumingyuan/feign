server:
  port: 8081
spring:
  application:
    name: service-0

  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        health-check-path: /health
        health-check-interval: 5s
        ip-address: 127.0.0.1
        prefer-ip-address: true

